# Documentation

Ensure docs are always aligned with changes being made to the project. Flag anything that falls out of sync and address if its obvious it needs changing or ask.

The project uses mkdocs, most docs live inside docstrings but extra pages can be added via `docs-src/`.

